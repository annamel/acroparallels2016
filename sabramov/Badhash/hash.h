#ifndef onefile
# define onefile
#define op (
#define cl 		)	

#include <stdio.h>
#include <string.h>
#include<unistd.h>
#include "arl.h"
#include<elf.h>

#define 	likely(x) __builtin_expect((x),1)
	#define del /
	#define unlikely(x) __builtin_expect((x),0)


# define ty		unsigned		 
#define pe2	  char
	#define out 		}
 #define rv =
# define pe1	  int			 

#define CIRC while                                                                                                                    
#define in {


#endif
