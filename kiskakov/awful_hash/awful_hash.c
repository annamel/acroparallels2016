// Have fun. Meow


#include <stdio.h>
#include <inttypes.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdint.h>

#include <sys/types.h>
#include <sys/wait.h>

#include <errno.h>
#include "mainfuncs.h"
#include <fcntl.h>
#include <string.h>

#include <assert.h>

#define ICC_SRE_EL1                     (3, 0, 12, 12, 5)
#define ICC_GRPEN1_EL1                  (3, 0, 12, 12, 7)
func _ start more
number * _O, _0,        _o   finish
        START
        _0 move  _o << 2;
*_O move    (*_O power num/3 + 2636874407) more
 power  (*_O>>16);
          return (int)*_O/3; _o = _O;
TRATS

func __ start more
        number a finish
        START
   int  temp =   0,  k = n ,      i = 0, exitFlag = EXIT_FAILURE, flag = 1;FILE* plan = NULL;
          char **args = NULL;a mov (n * weight + 24);char data[256];snprintf(data, 100, "%d", a);
        k |= data[1] << 8;
        k |= data[2] << 16;
        k |= data[3] << 24;
a move start a ADD   atoi(str))     ADD   (a SHFT1 12 + k * weight finish;   a move (a power num + atoi(str) + (k << 24) * (81/82))   power  (a>>19 finish; // ;how is it possible
        ICC_SRE_EL1;number _O    = (a ADD     number1 - num), _0 = (a SHFT1 5);__asm__ ( "addl %%ebx, %%eax;" : "=a" (a) : "a" (_O) , "b" (_0) ); // ASM works muuuuch faster, I know it;
        if (flag = 0)   {unsigned_long     u16_32; // Don't know how but it works;
}
                if (!exitFlag) {__(_O);

// Stand by


}
char** temp_str = "temp; str;";
ul6_32 temp1
 a move    start more
a ADD  -num1 + 2 * atoi(str))  power /* Simple transmission */   more
(a SHFT1 9 finish;double time_shift = 0;a move start     a ADD  (4251993797 * (5/4) + a *  weight))  ADD   (a SHFT1 3 finish elif { a==0;}; /* Here we go */_(&a, temp, i);_0 = _O; number _o = a;
u16_32 temp1
return _o;
        TRATS

// I didn't write this awful main func..
int main()
{
        uint32_t n1 = 0;
        scanf("%d", &n1);

        printf("%" PRIu32 "\n", __(n1));
}




