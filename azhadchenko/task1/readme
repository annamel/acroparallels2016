Программа bizzbuzz все числа в строке, которые
1. делятся на 3 заменяет на bizz
2. делятся на 5 заменяет на buzz
3. одновременно на 3 и на 5 - на bizzbuzz
В остальных случаях программа оставляет число нетронутыми.
Корректными являются числа n или -n, n принадлежит множеству натуральных чисел. Все остальное считается некорректным и не изменяется.
Программа написана для 64 разрядного процессора и предполагает наличие интерпретатора sh в /bin/sh.

Предполагается 2 формата ввода
1. Программа bizzbuzz запускается с параметрами, разделенными пробелами
Пример: ./bizzbuzz 1 2 3 4 5


2. В файл в одну строку записывается последовательность, подлежащая обработке. Вторая и последующие строки будут проигнорированы.
Далее запускается скрипт file_inputer, параметром которого является путь к файлу.

Пример:
echo "1 2 3 4 5" > test
./file_inputer test

Обратите внимание, что программа bizzbuzz должна находится в той же папке, что и скрипт.
